import type { Track, SearchResult } from "ziplayer";
import type { LavalinkRawTrack } from "../types/lavalink";
export declare class TrackResolver {
    private debug;
    constructor(debug: boolean);
    mapToTrack(raw: LavalinkRawTrack, requestedBy: string): Track;
    resolveTrackFromLavalink(player: any, raw: LavalinkRawTrack): Track | null;
    ensureTrackEncoded(player: any, track: Track, requestedBy: string, nodeManager: any): Promise<void>;
    resolvePlayRequest(player: any, query: string | Track, requestedBy: string, nodeManager: any, searchPrefix?: string): Promise<{
        tracks: Track[];
        isPlaylist: boolean;
    }>;
    searchLavalink(query: string, requestedBy: string, nodeManager: any, searchPrefix?: string): Promise<SearchResult>;
}
//# sourceMappingURL=TrackResolver.d.ts.map